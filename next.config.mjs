/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, 
    images : { 
    domains : ['i.ibb.co.com', 'images.pexels.com', 'i.imghippo.com']
  } 
};

export default nextConfig;
